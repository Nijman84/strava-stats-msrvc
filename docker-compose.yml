services:
  pull:
    build: .
    env_file: .env
    working_dir: /app
    volumes:
      - ./data:/app/data
      - ./secrets:/app/secrets
    user: "${UID:-1000}:${GID:-1000}"
    environment: [ "PYTHONUNBUFFERED=1" ]